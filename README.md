# LeetCode Submissions

My accepted LeetCode solutions, synced automatically from my submission history.
Problems are grouped by their primary topic tag.

_Not synced yet — run the backfill and this page becomes a full index:_

```
cd _sync
pip install -r requirements.txt
cp .env.example .env      # then paste your cookies in
python -m leetcode_sync --full
```

See [`_sync/README.md`](_sync/README.md) for how it works.
